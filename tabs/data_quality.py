import streamlit as st
import pandas as pd


def render(records: pd.DataFrame, candidates: pd.DataFrame, pages: pd.DataFrame, official: pd.DataFrame) -> None:
    st.info("🚧 Data Quality tab — placeholder (assigned to another team member)")
